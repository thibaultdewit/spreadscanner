"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var bignumber_js_1 = require("bignumber.js");
var Trade = /** @class */ (function () {
    function Trade() {
    }
    Trade.fromJSON = function (tradeJson) {
        var trade = new Trade();
        trade.id = tradeJson.id;
        trade.maker = tradeJson.maker;
        trade.taker = tradeJson.taker;
        trade.price = new bignumber_js_1.BigNumber(tradeJson.price);
        trade.amount = new bignumber_js_1.BigNumber(tradeJson.amount);
        trade.takerPrice = new bignumber_js_1.BigNumber(tradeJson.takerPrice);
        trade.feeAmount = new bignumber_js_1.BigNumber(tradeJson.feeAmount);
        trade.buyer = tradeJson.buyer;
        trade.seller = tradeJson.seller;
        trade.pair = tradeJson.pair;
        trade.status = tradeJson.status;
        trade.executedAt = new Date(tradeJson.executedAt);
        trade.createdAt = new Date(tradeJson.createdAt);
        trade.transactionId = tradeJson.transactionId;
        return trade;
    };
    return Trade;
}());
exports.Trade = Trade;
